# BINPREF defined here (requires Rtools in folder /App/Rtools).
Sys.setenv(BINPREF = "")

# Add path for CmdStan (requires compiled CmdStan program in folder /App/CmdStan
# and R package cmdstanr).
if (file.exists("")) {
cmdstanr::set_cmdstan_path("")
}

# Set working directory here instead of RStudio
#setwd("")
